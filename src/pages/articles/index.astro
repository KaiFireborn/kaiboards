---
import { getCollection } from 'astro:content'
import BlogCard from '../../components/BlogCard.astro'
import BaseLayout from '../../layouts/BaseLayout.astro'

const posts = (await getCollection('articles')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
)
---

<BaseLayout>
	<main class="app-container pt-36">
		<h1 class="text-6xl pb-8 font-bold">Articles</h1>
		<p class="md:w-2/3 text-lg leading-[2rem]">
			Some articles on aspects of ergonomic keyboards/layouts and notes on the process of choosing, designing or tweaking one based on my own experience.
		</p>
	</main>
	<section class="app-container py-12">
		<ul class="posts-grid-container">
			{
				posts.map((post) => (
					<li>
						<BlogCard post={post} />
					</li>
				))
			}
		</ul>
	</section>
</BaseLayout>
